/**
 * Loads and sets the picture 
 */
function show_picture(image_el, id, image_url) {
	if(!image_el|| !id ) {
		return;
	}
	if(image == undefined) {
		//get the image
	} else {
		//show the image
		//image_el.attr("src") = image_url;
	}
	
}